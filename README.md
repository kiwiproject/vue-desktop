# Vue Desktop

Lightweight Vue 3 + TypeScript desktop-style window manager library (plugin-based).

Getting started

1. Install dependencies: `pnpm install`
2. Run local demo: `pnpm --filter ./examples/demo dev`

See `DESIGN.md` for architecture and goals.
